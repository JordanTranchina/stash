# Everything in here is generated — see README.md. Leading slashes anchor each
# pattern to mobile/, so the checked-in sources under native/ios and
# native/android are not swept up with the generated ios/ and android/ projects.

# Built from web/ by `npm run build`. Never edit here — edit web/.
/www/

# Generated by `npx cap add ios|android`, then customized by
# scripts/apply-native-overlays.js. Regenerate with `npm run init`.
/ios/
/android/

/node_modules/
